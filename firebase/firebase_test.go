package firebase

import (
	"testing"
)

func TestPlatformReturnsErrorOnProjectError(t *testing.T) {
	// ctrl := gomock.NewController(t)
	// defer ctrl.Finish()

	// mockProject := NewMockFirebaseProject(ctrl)

	// mockProject.EXPECT().Create("projectId").Return(gcp.Project{}, errors.New("error"))

}
